# MPV 播放器配置文件
# ======================

# 窗口设置
border=no                    # 无边框窗口

# 视频输出设置
vo=gpu-next                  # 视频输出驱动。许多后续选项也只能在此三项下正常工作。当前版本默认值即 gpu-next
gpu-api=d3d11                # GPU API 设置
hwdec=auto-copy              # 硬件解码设置

# 窗口大小设置
geometry=1280x720            # 统一窗口大小设置 - 保持一致的初始窗口大小为 1280x720